apiVersion: v1
kind: Service
metadata:
  name: {{ include "zm-test-non-branch2.fullname" . }}
  labels:
    {{- include "zm-test-non-branch2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zm-test-non-branch2.selectorLabels" . | nindent 4 }}
