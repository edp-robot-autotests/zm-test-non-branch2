apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zm-test-non-branch2.fullname" . }}-test-connection"
  labels:
    {{- include "zm-test-non-branch2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zm-test-non-branch2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
